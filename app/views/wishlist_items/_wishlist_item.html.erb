<div id="<%= dom_id(item) %>" class="flex items-start justify-between p-4 bg-gray-50 rounded-lg">
  <div class="flex-1">
    <p class="font-medium text-gray-900"><%= item.description %></p>
    <div class="flex items-center gap-4 mt-2 text-sm text-gray-600">
      <% if item.url.present? %>
        <%= link_to "View Link →", item.url, target: "_blank", class: "text-blue-600 hover:text-blue-800" %>
      <% end %>
      <% if item.price.present? %>
        <span>💰 <%= item.price %></span>
      <% end %>
    </div>
  </div>
  <div>
    <%= button_to wishlist_item_path(item),
        method: :delete,
        data: { turbo: true, turbo_confirm: "Remove this item?" },
        class: "text-red-600 hover:text-red-700 font-semibold text-sm" do %>
      Remove
    <% end %>
  </div>
</div>
